/**
 * SLF4J bridge that routes trace events through the logging framework with MDC integration.
 *
 * <p>{@link ai.narrativetrace.slf4j.Slf4jNarrativeContext} is a {@code NarrativeContext}
 * implementation that logs trace events via SLF4J and propagates trace IDs through MDC.
 */
package ai.narrativetrace.slf4j;
